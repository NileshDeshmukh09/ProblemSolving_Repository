/**
 * 
 * Linkedlist is a linear  data structure , which has node , it contains DATA and ADRESS to next Node.
 * 
 * We cannot change array in Runtime arr[10] , such that arr size cannot be change in 100 size in Runtime .
 * 
 * ADVANTAGE
 * 
 * 1. Dynamic DS ( grow / shrink ) in Runtime 
 * 2. Memory wastage nahi hogi 
 * 3. Deletion is easy , because isme element shift nahi krna padta hai.
 * 4. Non Continous DS , can grow and shrink in Run time
 * /